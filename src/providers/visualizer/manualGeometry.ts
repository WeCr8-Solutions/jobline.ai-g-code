// Manual geometry creation for stock/fixture (box, cylinder, etc.)
export function createBoxGeometry(width: number, height: number, depth: number): any {
  // TODO: Return geometry object for three.js or similar
  return {};
}

export function createCylinderGeometry(radius: number, height: number): any {
  // TODO: Return geometry object for three.js or similar
  return {};
}
