import { BufferGeometry } from 'three';

// Manual geometry creation for stock/fixture (box, cylinder, etc.)
export function createBoxGeometry(_width: number, _height: number, _depth: number): BufferGeometry {
  // TODO: Return geometry object for three.js or similar
  return new BufferGeometry();
}

export function createCylinderGeometry(_radius: number, _height: number): BufferGeometry {
  // TODO: Return geometry object for three.js or similar
  return new BufferGeometry();
}
